/*
  1. 运行nodejs代码
    - 右键 run
    - 在cmd / terminal中，来到当前文件所在目录。 运行 node 文件名
  2. nodejs 是使用js开发的。
    js组成：
      DOM 没有DOM
      BOM 包含少量的BOM
        console
        setTimeout
        setInterval
      ES 包含基本上所有的ES

 */
console.log('hello world');

// console.log(document);
// console.log(window);
console.log(global); // 全局对象